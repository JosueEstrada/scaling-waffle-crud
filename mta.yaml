_schema-version: "3.2"
ID: appdemotest
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: appdemotest-destination-content
  type: com.sap.application.content
  requires:
  - name: appdemotest-destination-service
    parameters:
      content-target: true
  - name: appdemotest-repo-host
    parameters:
      service-key:
        name: appdemotest-repo-host-key
  - name: appdemotest-uaa
    parameters:
      service-key:
        name: appdemotest-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: appdemotest_html_repo_host
          ServiceInstanceName: appdemotest-html5-srv
          ServiceKeyName: appdemotest-repo-host-key
          sap.cloud.service: appdemotest
        - Authentication: OAuth2UserTokenExchange
          Name: appdemotest_uaa
          ServiceInstanceName: appdemotest-xsuaa-srv
          ServiceKeyName: appdemotest-uaa-key
          sap.cloud.service: appdemotest
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: appdemotest-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: appdemotest-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - appdemotest.zip
      name: appdemotest
      target-path: resources/
- name: appdemotest
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: appdemotest-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: appdemotest-destination-service
    service-plan: lite
- name: appdemotest-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: appdemotest-xsuaa-srv
    service-plan: application
- name: appdemotest-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: appdemotest-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
